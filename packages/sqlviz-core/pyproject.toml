[project]
name = "sqlviz-core"
version = "0.2.1"
description = "SQLviz kernel: contracts, registries, shared models"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sqlviz_core"]
